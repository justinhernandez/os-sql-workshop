{
  "name": "sql-demo",
  "version": "0.0.1",
  "description": "OS LnL SQL Demo",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch . -e js",
    "pretty": "yarn prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "faker": "^5.5.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prettier": "^2.3.0"
  }
}
